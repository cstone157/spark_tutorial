{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "jupyter" }}
  namespace: {{ .Release.Name }}
  labels:
    app: {{ printf "jupyter" }}
spec:
  type: NodePort # ClusterIP
  ports:
    - port: {{ .Values.port }}
      name: jupyter
  selector:
    app: {{ printf "jupyter" }}
{{- end }}